"use strict";

exports.__esModule = true;
exports.prepareRegex = void 0;
const prepareRegex = str => {
  var _ref;
  const exploded = str.split(`/`);
  const regex = new RegExp(exploded.slice(1, -1).join(`/`), // last char
  (_ref = [...exploded][exploded.length - 1]) === null || _ref === void 0 ? void 0 : _ref[0]);
  return regex;
};
exports.prepareRegex = prepareRegex;
//# sourceMappingURL=prepare-regex.js.map